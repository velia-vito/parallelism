name: processors
description: setup isolates with 2-way communication with minimal overhead and maximum speed
version: 1.0.2

repository: https://github.com/MikhailZex/processors

environment:
  sdk: '>=2.12.0 <3.0.0'

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  lints: ^1.0.1
  test: ^1.16.0
